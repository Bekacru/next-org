import { Button } from "./ui/button";
import { Dialog, DialogTrigger } from "./ui/dialog";

const CreateTeam = () => {
  return (
    <Dialog>
      <DialogTrigger>
        <Button>Create Team</Button>
      </DialogTrigger>
    </Dialog>
  );
};
